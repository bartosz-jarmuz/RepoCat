@using RepoCat.Portal.Areas.Catalog.Views.Components
@model RepoCat.Portal.Areas.Catalog.Views.Components.NavHeaderStatsViewModel

@{
    string badgeClass = "badge badge-info badge-pill badge-inherit";
}

    <li class="nav-item px-3 ">
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.OrganizationsCount</span>
            <span class="small">Organizations</span>
        </div>
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.RepositoriesCount</span>
            <span class="small">Repositories</span>
        </div>
    </li>
    <li class="nav-item px-3 ">
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.ProjectsCount</span>
            <span class="small">Projects</span>
        </div>
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.ComponentsCount</span>
            <span class="small">Components</span>
        </div>
    </li>
    <li class="nav-item px-3 ">
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.TagsCount</span>
            <span class="small">Tags</span>
        </div>
        <div class="nav-link" style="text-align: left !important;">
            <span class="@badgeClass">@Model.PropertiesCount</span>
            <span class="small">Properties</span>
        </div>
    </li>
